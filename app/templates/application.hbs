<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Meritmoney</a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-right">

          <li>
          {{#if session.isAuthenticated}}<img src={{session.currentUser.imageUrl}} class="img-responsive img-circle" width="50" height="50" />
          {{/if}}
          </li>
      <li>  {{#if session.isAuthenticated}}

          <button class="btn navbar-btn btn-danger" {{action "signOut"}}>Sign out</button>
          {{outlet}}
        {{else}}
          <button class="btn navbar-btn btn-success" {{action "signIn" "google"}}>Sign in</button>
        {{/if}} </li>

        <li><a href="#">Dashboard</a></li>
        <li><a href="#">Profile</a></li>

      </ul>

    </div>
  </div>
</nav>

<div class="container-fluid">
  <div class="row">
    <div class="col-sm-3 col-md-2 sidebar">
      <ul class="nav nav-sidebar">
        <li class="active"><a href="#">Overview <span class="sr-only">(current)</span></a></li>
        <li><a href="#">Reports</a></li>
        <li><a href="#">Analytics</a></li>
      </ul>
     </div>
    <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
      <h1 class="page-header">Dashboard </h1>
<div class="row placeholders">
{{#if session.isAuthenticated}}

        {{#each model as |user|}}
        <div class="col-xs-6 col-sm-3 placeholder">
          <img src={{user.imageUrl}} width="200" height="200" class="img-responsive" alt="Generic placeholder thumbnail">
          <h4>{{user.name}}</h4>
          <span class="text-muted">Something else</span>
        </div>
        {{/each}}
        {{else}}
        <div class="jumbotron">
          <h1> Please sign in!</h1>
        </div>

        {{/if}}
</div>
</div>


    </div>
  </div>


{{outlet}}
